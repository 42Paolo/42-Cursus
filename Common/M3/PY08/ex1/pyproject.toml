[tool.poetry]
name = "matrix-loader"
version = "1.0.0"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10"
matplotlib = ">=3.7.2"
requests = ">=2.31.0"
numpy = ">=1.25.0"
pandas = ">=2.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"